package com.bptn.course._14_access_modifiers._02_default._01_classes._01_same_package;

/*
 * The default access modifier is also know as the package access modifier because
 * it allows visibility with the package ONLY.
 *
 * When we don't specify any access modifier, then the access modifier is "default"
 */

class MyClass {

}
